# Login page (preview)

Files created:

- [index.html](index.html)
- [styles.css](styles.css)
- [script.js](script.js)
- [assets/logo.svg](assets/logo.svg)

Preview locally:

```bash
python -m http.server 8000
# open http://localhost:8000/index.html
```

Using the attached logo image:

- Save the attached logo image into the `assets` folder as `logo.png`.
- Then edit `index.html` and change the image `src` to `assets/logo.png` (or replace `assets/logo.svg` with your PNG and keep the filename).

If you want, I can embed your attached PNG into the page directly (as `assets/logo.png`).
